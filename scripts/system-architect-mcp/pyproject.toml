[project]
name = "kyron-system-architect-public"
version = "1.1.0"
requires-python = ">=3.12"
dependencies = [
  "mcp==2.0.0",
  "pydantic>=2.12,<2.14",
  "starlette>=0.47,<1",
]

[tool.vercel]
entrypoint = "app:app"
